import styled from "styled-components";
import { MARGIN } from "@/utils/theme.ts";

export const StyledTextInput = styled.input`
  margin-left: ${MARGIN.s};
`;
export const Container = styled.div`
  display: flex;
`;
